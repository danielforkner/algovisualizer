export default function QuickSort() {
  return (
    <div>
      <h2>Quick Sort</h2>
      <canvas id="quickSort"></canvas>
      {/* <div className="end-time">
        {endTime
          ? `Time to sort: ${(
              (endTime - startTime - speed * waitCount) /
              1000
            ).toFixed(3)}s`
          : null}
      </div> */}
    </div>
  );
}
